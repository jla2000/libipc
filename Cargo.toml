[package]
name = "libipc"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.3", features = ["fs", "mman"] }
thiserror = "2"
crossbeam-utils = "0.8"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "throughput"
harness = false
